// Write a function called make_avg() which will take an array of integers and the size of that array and return the average of those values.

function make_avg(numbers) {
    let sum = 0;
    len = numbers.length;
    for (num of numbers){
    zog = sum + num;
    }
    average = zog / len;
    return zog
}

const array = [1, 2, 15, 4, 54, 64, 63, 41, 21, 12, 43, 85]
make_avg(array)

console.log("The average of the aray is :", average)